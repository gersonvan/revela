---
stage: 1
task: 2
title: Audit Operational Baseline
agent: qa-release-agent
status: Partial
important_findings: true
compatibility_issues: false
---

# Task 1.2 - Audit Operational Baseline

## Summary
Established the current operational baseline before Fase 2 work: local lint/typecheck/build pass, public production routes are available, protected admin/export/QR routes redirect to login without a session, and documentation now records the evidence and remaining caveats.

## Details
Worked in `/Users/gersonvan/Documents/EventoOn/.apm/worktrees/codex-audit-operational-baseline` on branch `codex/audit-operational-baseline`. Inspected current Git state, package scripts, validation/deploy documentation, and route surface. Installed dependencies in the worktree because the first lint attempt failed before execution due to missing `node_modules`. Ran local validation and production HTTP checks without using or exposing private credentials. Updated the implementation status document and added a dated QA note.

Committed the documentation changes on the assigned branch:

- Commit: `6c5157c` (`Document operational baseline`)

## Output
- Modified: `docs/STATUS_IMPLEMENTACAO.md`
- Added: `docs/QA-2026-07-05-baseline-operacional.md`

No product behavior or application code was changed.

## Validation
- `pnpm install`: passed in the assigned worktree.
- `pnpm lint`: passed.
- `pnpm typecheck`: passed.
- `pnpm build`: passed.
- `pnpm env:check`: failed because the worktree has no local `.env`; missing variables were `DATABASE_URL`, `NEXTAUTH_SECRET`, `NEXTAUTH_URL`, `GOOGLE_CLIENT_ID`, and `GOOGLE_CLIENT_SECRET`.
- Production `/`: `HTTP 200`.
- Production `/e/ensaio-producao-kkh7uc`: `HTTP 200`.
- Production `/screen/ensaio-producao-kkh7uc`: `HTTP 200`.
- Production `/t/ensaio-producao-kkh7uc`: `HTTP 307` to `/screen/ensaio-producao-kkh7uc`.
- Production `/api/events/ensaio-producao-kkh7uc/approved-photos`: `HTTP 200`, JSON payload with `photos` containing 2 items at audit time.
- Production `/moderate`: `HTTP 200`.
- Production `/admin`: `HTTP 307` to `/admin/login?callbackUrl=%2Fadmin` without session.
- Production `/admin/login`: `HTTP 200`.
- Production protected export/ZIP/QR paths under `/admin/events/nonexistent/...`: `HTTP 307` to login without session.
- Final worktree status after commit: clean on `codex/audit-operational-baseline`.

## Issues
Full operational validation remains incomplete without credentials/session and environment:

- Admin-authenticated export, QR, event management, and real ZIP download were not validated in this no-session baseline.
- Moderator token flow was not validated with a private token.
- `pnpm smoke:demo` was not run because the worktree lacks local `.env`, database setup, and seeded demo data.

## Important Findings
- The assigned worktree initially had no `node_modules`; validation required `pnpm install` before lint/typecheck/build could run.
- The public production baseline is healthy for home, upload page, screen route, short screen redirect, and approved photos feed.
- Protected admin/export/QR behavior without session is correct and should not be interpreted as a production failure.
- Remaining readiness work for the 2026-07-11 event should prioritize authenticated end-to-end rehearsal and physical mobile QR testing.
