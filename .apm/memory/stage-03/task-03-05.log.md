---
stage: 3
task: 5
title: App Build Distribution Plan
agent: qa-release-agent
status: Partial
important_findings: true
compatibility_issues: true
---

# Task 3.5 - App Build Distribution Plan

## Summary
Prepared the moderator app build/distribution plan and recorded current validation results. App and root typechecks pass, root build passes, and documentation now clearly separates what can be tested before store accounts from what requires Apple/Google/EAS and physical devices.

## Details
Worked in `/Users/gersonvan/Documents/EventoOn/.apm/worktrees/codex-app-build-distribution-plan` on branch `codex/app-build-distribution-plan`. Inspected `apps/moderator`, app README/config/scripts, moderator app architecture/API docs, backend `/api/moderator-app/*` routes, push-token registration, and the integrated API smoke script. Created a dedicated runbook for build, test, and distribution, then linked it from the app README and architecture doc.

Committed documentation changes on the assigned branch:

- Commit: `88b4536` (`Document moderator app distribution`)

## Output
- Added: `docs/APP_MODERADOR_BUILD_DISTRIBUICAO.md`
- Modified: `apps/moderator/README.md`
- Modified: `docs/ARQUITETURA_APP_MODERADOR.md`

No app behavior, backend code, package versions, or lockfile entries were changed.

## Validation
- `pnpm install`: passed.
- `pnpm --filter @eventoon/moderator-app typecheck`: passed.
- `pnpm typecheck`: passed.
- `pnpm lint`: passed with 2 warnings in `apps/moderator/App.tsx`:
  - React hook missing dependencies: `apiBaseUrl`, `showError`.
  - React Native `Image` warning from web/a11y lint requiring `alt`.
- `pnpm build`: passed and included `/api/moderator-app/*` routes.
- `pnpm --filter @eventoon/moderator-app exec expo --version`: returned `57.0.4`.
- `pnpm --filter @eventoon/moderator-app exec expo config --type public`: passed and confirmed `Revela Moderador`, slug `revela-moderador`, scheme `revela-moderador`, Expo SDK `57.0.0`, platforms `ios/android/web`, and `expo-notifications`.
- `pnpm --filter @eventoon/moderator-app exec expo install --check`: failed because Expo SDK 57 expects `react@19.2.3` and `typescript@~6.0.3`, while the workspace has `react@19.2.4` and `typescript@5.9.3`.
- `pnpm --filter @eventoon/moderator-app exec eas --version`: initially failed because `eas` was not installed; after user approval, `eas-cli` was installed and now reports `eas-cli/20.5.1 darwin-arm64 node-v24.15.0`.
- `git diff --check`: passed.
- Final worktree status after commit: clean on `codex/app-build-distribution-plan`.

## Issues
- No `eas.json` exists.
- `eas-cli` is now installed in `@eventoon/moderator-app`, but no EAS project/configuration has been created.
- Expo dependency compatibility check currently fails.
- Real push-token generation requires physical device, OS permissions, and Expo/EAS project/credentials.
- App Store/TestFlight and Google Play/Internal Testing still require external accounts, EAS configuration, signing setup, review, and timing outside repo control.

## Compatibility Concerns
The app package currently passes TypeScript but does not pass `expo install --check` for Expo SDK 57 dependency alignment. I did not change versions because doing so would affect shared workspace dependencies and should be handled as an explicit dependency task.

## Manager Review Note

Accepted as Stage 3 completion after correcting the generated runbook formatting. The versioned `eas-cli` dependency was removed during review because the resulting lockfile failed the current `pnpm` supply-chain policy on a newly published transitive dependency (`multipasta@0.2.8`). EAS remains documented as a required build/distribution setup step, not a ready project dependency.

## Important Findings
- The safe plan for the 2026-07-11 event remains web-first: QR upload, web moderation, screen, and ZIP/export.
- The app can be tested before store accounts via typecheck, root build, Expo Go/dev server, and the existing `pnpm smoke:moderator-app` when backend/database/test data are available.
- Store distribution cannot be promised without Apple Developer/Google Play Developer accounts, signing setup, EAS project/configuration, physical-device tests, and external review.
- Backend push-token persistence exists, but backend push dispatch is not implemented in this phase.
