---
stage: 5
task: 1
title: Final Smoke Production Checks
agent: qa-release-agent
status: Partial
important_findings: true
compatibility_issues: false
---

# Task 5.1 - Final Smoke Production Checks

## Summary
Completed the final smoke report for the 2026-07-11 event readiness check. Local typecheck/build checks passed and public production routes are available; status is Partial because database-backed smokes, authenticated admin/export/moderator flows, and physical mobile checks remain blocked by unavailable local Postgres/Docker or missing private session/token access.

## Details
Worked in `/Users/gersonvan/Documents/EventoOn/.apm/worktrees/codex-final-smoke-production-checks` on branch `codex/final-smoke-production-checks`. Installed dependencies, ran local validation, attempted database smokes, checked production public routes without secrets, reviewed moderator app and video-proof status, then documented the final smoke evidence.

Committed documentation changes:

- Commit: `64a54b9` (`Document final production smoke`)

## Output
- Added: `docs/QA-2026-07-06-smoke-final-producao.md`
- Modified: `docs/STATUS_IMPLEMENTACAO.md`

No product behavior or application code was changed.

## Validation
- `pnpm install`: passed.
- `pnpm typecheck`: passed.
- `pnpm build`: passed and included the web routes, admin/export/QR routes, screen/upload APIs, and `/api/moderator-app/*`.
- `pnpm --filter @eventoon/moderator-app typecheck`: passed.
- `pnpm lint`: passed with 2 known warnings in `apps/moderator/App.tsx`.
- `pnpm env:check`: failed because `.env` is absent in the worktree; missing `DATABASE_URL`, `NEXTAUTH_SECRET`, `NEXTAUTH_URL`, `GOOGLE_CLIENT_ID`, and `GOOGLE_CLIENT_SECRET`.
- `pnpm --filter @eventoon/moderator-app exec eas --version`: failed in this branch because `eas` is not versioned here.
- `docker compose up -d postgres`: failed because Docker daemon was unavailable at `unix:///Users/gersonvan/.docker/run/docker.sock`.
- `pnpm db:migrate`: failed because local Postgres was unavailable.
- `pnpm smoke:moderation-mode`: failed with `ECONNREFUSED 127.0.0.1:5432`.
- `pnpm smoke:moderator-app`: failed with `ECONNREFUSED 127.0.0.1:5432`.
- `node scripts/video-proof.mjs --help`: passed and confirmed input/FFmpeg requirements.
- Production `/`: `HTTP 200`.
- Production `/e/ensaio-producao-kkh7uc`: `HTTP 200`.
- Production `/screen/ensaio-producao-kkh7uc`: `HTTP 200`.
- Production `/t/ensaio-producao-kkh7uc`: `HTTP 307` to `/screen/ensaio-producao-kkh7uc`.
- Production `/api/events/ensaio-producao-kkh7uc/approved-photos`: `HTTP 200`, payload with `photos` containing 2 items at smoke time.
- Production `/admin`: `HTTP 307` to login without session.
- Production `/admin/login`: `HTTP 200`.
- Production protected JSON/ZIP/QR routes under `/admin/events/nonexistent/...`: `HTTP 307` to login without session.
- Production `/moderate`: `HTTP 200`.
- Final worktree status after commit: clean on `codex/final-smoke-production-checks`.

## Issues
- Authenticated admin, moderator-token, real ZIP/export, and physical mobile upload checks were not run because no valid session/token/environment was available in the task context.
- Database-backed smokes remain blocked by unavailable Docker/Postgres in this environment.
- The moderator app remains non-critical for the event and not store-ready in this branch.
- Video remains a controlled proof and was not re-run because no local sample exists under `storage/video-proof/`.

## Important Findings
- Public production surface for home, guest upload, screen, short screen redirect, and approved feed is available as of this smoke.
- Protected admin/export/QR routes correctly redirect to login without session; this is expected, not a failure.
- The practical recommendation for 2026-07-11 is to use the web flow as the critical path: QR Code -> upload web -> moderação web -> telão -> exportação ZIP.
- App native and video proof should stay outside the event-critical path unless separately validated with real devices, credentials, and production-equivalent backend access.
